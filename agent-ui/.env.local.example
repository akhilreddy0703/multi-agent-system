# Agent UI — connect to Multi-Agent RAG backend
NEXT_PUBLIC_AGENTOS_ENDPOINT=http://localhost:8000

# Optional: pre-set auth token (otherwise use the login redirect or paste token in the UI)
# NEXT_PUBLIC_OS_SECURITY_KEY=your_jwt_here
